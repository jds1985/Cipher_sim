// pages/api/fb_exchange.js
// Temporary stub to avoid build errors.
// We’re not using this endpoint right now.

export default function handler(req, res) {
  return res.status(200).json({
    ok: true,
    message: "fb_exchange endpoint not implemented yet.",
  });
}
